# Myntra-Studio-Prototype

# Run in PC, Android rn has some issues with stack navigator
1. Install Expo
```npm install expo```
2. Clone the repo
3. Inside the folder, do 
```npm install```
4. Start expo
```expo start -c```
5. In the browser, select option "start in web browser" 
